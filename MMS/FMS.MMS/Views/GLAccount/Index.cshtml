@model  IEnumerable<MMS.Entities.Models.FMS_GLAccount>
@using System.Text.RegularExpressions
@{
    ViewBag.Title = "GL Account Tree";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section breadcrumb
          {
    <li><a href="~/Dashboard/index">Dashboard</a></li>
    <li>Administration</li>
    <li class="active">GL Account Tree</li>
}
@section title
{
    @ViewBag.Title
}


@*@Html.ActionLink("Create New", "Create", new { })*@ @*<text>|</text>*@

<table class="table table-hover">
    <tr>

        <th>
            GLAccount Code
        </th>
        @*<th>
            Parent Account Name
        </th>*@
        <th>
            GLAccount Name
        </th>

       <th>
           Current Balance
           </th>

        <th>
            Is Active
        </th>
        
        <th>
            Has Child
        </th>

        <th>
            Is Subsidy Exist
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.GLAccountCode)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.FMS_GLAccount2.GLAccountTreeName)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem =>(item.GLAccountTreeName))
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentBalance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsActive)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HasChild)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsSubsidyExist)
            </td>
            <td>

                @Html.ActionLink("Edit", "Edit", new { id = item.GLAccountId }, new { @class = "btn btn-success btn-xs" }) <text>|</text>

                @Html.ActionLink("Create", "Create", new { id = item.GLAccountId }, new { @class = "btn btn-success btn-xs" })
            </td>
        </tr>
    }

</table>
